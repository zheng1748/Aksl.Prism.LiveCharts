<UserControl x:Class="Aksl.Modules.LiveCharts.Events.Views.AddPointOnClickView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ei="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="900"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>

    </UserControl.Resources>

    <Grid Margin="5,5,5,5">
        <lvc:CartesianChart x:Name="chart"
                            Series="{Binding SeriesCollection}"
                            PointerPressedCommand="{Binding PointerDownCommand}" 
                            TooltipPosition="Hidden">
            <!--<ei:Interaction.Triggers>
                <ei:EventTrigger EventName="PointerDownCommand">
                    <ei:InvokeCommandAction Command="{Binding PointerDownCommand}"
                                            CommandParameter="{Binding Path=ItemsSource,ElementName=customerListView}" />
                </ei:EventTrigger>
            </ei:Interaction.Triggers>-->
        </lvc:CartesianChart>
    </Grid>

</UserControl>
