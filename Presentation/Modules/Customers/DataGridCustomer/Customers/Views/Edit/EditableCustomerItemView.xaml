<UserControl x:Class="Aksl.Modules.DataGridCustomer.Views.EditableCustomerItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:actions="clr-namespace:Aksl.Xaml.Behaviors;assembly=Aksl.Toolkit"
             mc:Ignorable="d"
             d:DesignHeight="250"
             d:DesignWidth="800">
    <UserControl.Resources>
        <ControlTemplate x:Key="errorTemplate">
            <DockPanel>
                <!--<Border BorderBrush="Red"
                        BorderThickness="1"
                        CornerRadius="2">-->
                <AdornedElementPlaceholder x:Name="Placeholder" />
                <!--</Border>-->
                <TextBlock Text="*"
                           Foreground="Red"
                           FontSize="16" />
                <TextBlock Foreground="Red"
                           Text="{Binding ElementName=Placeholder,Path=AdornedElement.(Validation.Errors)[0].ErrorContent}"
                           FontSize="16"
                           Margin="2,0,0,0" />
            </DockPanel>
        </ControlTemplate>

        <Style x:Key="textBoxInError"
               TargetType="{x:Type TextBox}">
            <Style.Triggers>
                <Trigger Property="Validation.HasError"
                         Value="true">
                    <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={x:Static RelativeSource.Self},Path=(Validation.Errors)[0].ErrorContent}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="30" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="30" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="30" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="6" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <!--<Grid.Resources>
            <DataTemplate DataType="{x:Type ValidationError}">
                <TextBlock FontStyle="Italic"
                           Foreground="Red"
                           HorizontalAlignment="Right"
                           Margin="0,1"
                           Text="{Binding Path=ErrorContent}" />
            </DataTemplate>
        </Grid.Resources>-->

        <!-- CUSTOMER TYPE -->
        <Label Grid.Row="0"
               Grid.Column="0"
               Content="Customer _type:"
               HorizontalAlignment="Right"
               Target="{Binding ElementName=customerTypeComboBox}" />
        <ComboBox x:Name="customerTypeComboBox"
                  Grid.Row="0"
                  Grid.Column="2"
                  Margin="0,0,200,0"
                  ItemsSource="{Binding Path=CustomerTypeOptions, Mode=OneTime}"
                  SelectedItem="{Binding Path=CustomerType, ValidatesOnDataErrors=True}"
                  Validation.ErrorTemplate="{x:Null}">
            <behaviors:Interaction.Triggers>
                <behaviors:EventTrigger EventName="Loaded">
                    <actions:SetFocusAction Target="{Binding ElementName=customerTypeComboBox}" />
                </behaviors:EventTrigger>
            </behaviors:Interaction.Triggers>
        </ComboBox>

        <!-- FIRST NAME-->
        <Label Grid.Row="2"
               Grid.Column="0"
               Content="FirstName:"
               HorizontalAlignment="Right"
               Target="{Binding ElementName=firstNameTxt}" />
        <TextBox x:Name="firstNameTxt"
                 Grid.Row="2"
                 Grid.Column="2"
                 Margin="0,0,200,0"
                 Text="{Binding Path=FirstName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,ValidatesOnDataErrors=True}"
                 VerticalContentAlignment="Center"
                 Validation.ErrorTemplate="{StaticResource errorTemplate}" />

        <!-- LAST NAME-->
        <Label Grid.Row="4"
               Grid.Column="0"
               Content="LastName:"
               HorizontalAlignment="Right"
               Target="{Binding ElementName=lastNameTxt}" />
        <TextBox x:Name="lastNameTxt"
                 Grid.Row="4"
                 Grid.Column="2"
                 Margin="0,0,200,0"
                 Text="{Binding Path=LastName, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                 VerticalContentAlignment="Center"
                 Validation.ErrorTemplate="{x:Null}" />

        <!-- E-MAIL-->
        <Label Grid.Row="6"
               Grid.Column="0"
               Content="Email:"
               HorizontalAlignment="Right"
               Target="{Binding ElementName=emailTxt}" />
        <TextBox x:Name="emailTxt"
                 Grid.Row="6"
                 Grid.Column="2"
                 Margin="0,0,200,0"
                 Text="{Binding Path=Email, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                 VerticalContentAlignment="Center"
                 Validation.ErrorTemplate="{StaticResource errorTemplate}" />

        <!-- SAVE BUTTON -->
        <!--<Button Grid.Row="8"
                Grid.Column="2"
                Command="{Binding Path=SaveCommand}"
                Content="Save"
                HorizontalAlignment="Right" 
                VerticalAlignment="Bottom"
                Margin="4,2"
                MinWidth="60" />-->
    </Grid>
</UserControl>
