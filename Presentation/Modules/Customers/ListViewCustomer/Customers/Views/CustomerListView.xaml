<UserControl x:Class="Aksl.Modules.ListViewCustomer.Views.CustomerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:akslToolkit="clr-namespace:Aksl.Toolkit.UI.Controls;assembly=Aksl.Toolkit"
             xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        <!--https://www.wpf-tutorial.com/listview-control/listview-grouping/-->
        
        <LinearGradientBrush x:Key="headerBackground"
                             StartPoint="0.5,0"
                             EndPoint="0.5,1">
            <GradientStop Color="#66000088"
                          Offset="0" />
            <GradientStop Color="#BB000088"
                          Offset="1" />
        </LinearGradientBrush>

        <!--<CollectionViewSource x:Key="groupCustomerSource"
                              Source="{Binding Path=AllCustomers}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="IsCompany" />
            </CollectionViewSource.GroupDescriptions>
            --><!--<CollectionViewSource.SortDescriptions>
                <scm:SortDescription PropertyName="IsCompany"
                                     Direction="Descending" />
                <scm:SortDescription PropertyName="DisplayName"
                                     Direction="Ascending" />
            </CollectionViewSource.SortDescriptions>--><!--
        </CollectionViewSource>-->

        <DataTemplate x:Key="checkBoxHeaderTemplate">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <CheckBox IsChecked="{Binding Path=DataContext.IsChoice,Mode=TwoWay,RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListView}}}"
                          VerticalContentAlignment="Center"
                          Margin="2,0,5,0" />
                <TextBlock Text="全选" />
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="customerHeaderTemplateArrowUp">
            <DockPanel>
                <TextBlock HorizontalAlignment="Center"
                           Text="{Binding}" />
                <Path x:Name="arrow"
                      StrokeThickness="1"
                      Fill="gray"
                      Data="M 5,10 L 15,10 L 10,5 L 5,10" />
            </DockPanel>
        </DataTemplate>
        
        <DataTemplate x:Key="customerHeaderTemplateArrowDown">
            <DockPanel>
                <TextBlock HorizontalAlignment="Center"
                           Text="{Binding }" />
                <Path x:Name="arrow"
                      StrokeThickness="1"
                      Fill="gray"
                      Data="M 5,5 L 10,10 L 15,5 L 5,5" />
            </DockPanel>
        </DataTemplate>
        
        <!--<GroupStyle x:Key="CustomerGroupStyle">
            <GroupStyle.HeaderTemplate>
                <DataTemplate>
                    <TextBlock x:Name="txt"
                               Background="{StaticResource headerBackground}"
                               FontWeight="Bold"
                               Foreground="White"
                               Margin="1"
                               Padding="4,2,0,2"
                               Text="People" />
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding Path=Name}"
                                     Value="True">
                            <Setter TargetName="txt"
                                    Property="Text"
                                    Value="Companies" />
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </GroupStyle.HeaderTemplate>
        </GroupStyle>-->

        <!--<DataTemplate x:Key="headerTemplate">
            <views:CustomerItemHeader x:Name="headerTextBlock"
                                      HorizontalAlignment="Stretch"
                                      Background="{StaticResource headerBackground}"
                                      Title="People" />
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding Path=Name}"
                             Value="True">
                    <Setter TargetName="headerTextBlock"
                            Property="Title"
                            Value="Companies" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>-->

        <!--<DataTemplate x:Key="groupHeaderTemplate">
            <StackPanel Orientation="Horizontal"
                        Background="{StaticResource headerBackground}">
                <TextBlock x:Name="headerTextBlock"
                           Text="People"
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="White"
                           Margin="1"
                           Padding="4,2,0,2"
                           VerticalAlignment="Center" />
                <TextBlock Text="{Binding ItemCount}"
                           FontSize="16"
                           Foreground="White"
                           FontWeight="Bold"
                           Margin="20,0,20,0"
                           VerticalAlignment="Center" />
                <TextBlock Text=" Item(s)"
                           FontSize="16"
                           Foreground="White"
                           FontWeight="Bold"
                           VerticalAlignment="Center" />
            </StackPanel>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding Path=Name}"
                             Value="True">
                    <Setter TargetName="headerTextBlock"
                            Property="Text"
                            Value="Companies" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>-->

        <Style x:Key="customerGridViewColumnHeaderStyle"
               TargetType="{x:Type GridViewColumnHeader}">
            <Setter Property="SnapsToDevicePixels"
                    Value="True" />
            <!--<Setter Property="MinWidth"
                    Value="0" />
            <Setter Property="MinHeight"
                    Value="28" />-->
            <Setter Property="Height"
                    Value="40" />
            <Setter Property="FontSize"
                    Value="18" />
            <Setter Property="FontWeight"
                    Value="Bold" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="HorizontalContentAlignment"
                    Value="Center" />
            <Setter Property="VerticalAlignment"
                    Value="Center" />
        </Style>
        
        <Style x:Key="customerGroupItemStyle"
               TargetType="{x:Type GroupItem}">
            <Setter Property="Background"
                    Value="{StaticResource headerBackground}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GroupItem}">
                        <!--Background="{StaticResource headerBackground}"-->
                        <Expander IsExpanded="True"
                                  BorderThickness="1,1,1,5">
                            <Expander.Header >
                                <StackPanel Orientation="Horizontal"  
                                            Height="24"
                                            VerticalAlignment="Stretch">
                                    <TextBlock x:Name="headerTextBlock"
                                               Text="{Binding Path=Name}"
                                               FontWeight="Bold"
                                               Foreground="Black"
                                               FontSize="16" />
                                    <TextBlock Text="{Binding ItemCount}"
                                               FontSize="16"
                                               Foreground="Green"
                                               FontWeight="Bold"
                                               FontStyle="Italic"
                                               Margin="10,0,0,0" />
                                    <TextBlock Text="   Item(s)"
                                               FontWeight="Bold"
                                               Foreground="Black"
                                               FontSize="16" />
                                </StackPanel>
                            </Expander.Header>
                            <Expander.Content>
                                <ItemsPresenter />
                            </Expander.Content>
                        </Expander>
                        <ControlTemplate.Triggers>
                            <DataTrigger Binding="{Binding Path=Name}"
                                         Value="True">
                                <Setter TargetName="headerTextBlock"
                                        Property="Text"
                                        Value="Companies" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Path=Name}"
                                         Value="False">
                                <Setter TargetName="headerTextBlock"
                                        Property="Text"
                                        Value="People" />
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="customerItemStyle"
               TargetType="{x:Type ListViewItem}">
            <!--<Setter Property="Background"
                    Value="White" />-->
            <Setter Property="Height"
                    Value="30" />
            <Setter Property="FontSize"
                    Value="15" />
            <Setter Property="HorizontalContentAlignment"
                    Value="Center" />
            <Setter Property="VerticalAlignment"
                    Value="Center" />
            <Setter Property="IsSelected"
                    Value="{Binding IsSelected,Mode=TwoWay}" />
            <Style.Triggers>
                <!--<MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="ItemsControl.AlternationIndex"
                                   Value="1" />
                        <Condition Property="IsSelected"
                                   Value="False" />
                        <Condition Property="IsMouseOver"
                                   Value="False" />
                    </MultiTrigger.Conditions>
                    <Setter Property="Background"
                            Value="#EEEEEEEE" />
                </MultiTrigger>-->
                <!--<MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected"
                                   Value="False" />
                    </MultiTrigger.Conditions>
                    --><!--<Setter Property="Background"
                            Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                    <Setter Property="Foreground"
                            Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}" />-->
                    <!--<Setter Property="Foreground"
                            Value="Black" />--><!--
                    <Setter Property="FontSize"
                            Value="15" />
                </MultiTrigger>-->
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected"
                                   Value="True" />
                        <Condition Property="Selector.IsSelectionActive"
                                   Value="True" />
                    </MultiTrigger.Conditions>
                    <!--<Setter Property="Background"
                            Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                    <Setter Property="Foreground"
                            Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}" />-->
                    <!--<Setter Property="Foreground"
                            Value="Black" />-->
                    <Setter Property="FontSize"
                            Value="16" />
                </MultiTrigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected"
                                   Value="True" />
                        <Condition Property="Selector.IsSelectionActive"
                                   Value="False" />
                    </MultiTrigger.Conditions>
                    <Setter Property="Background"
                            Value="#EEEEEEEE" />
                    <!--<Setter Property="Background"
                            Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />-->
                    <!--<Setter Property="Background"
                            Value="#EEEEEEEE" />-->
                    <Setter Property="FontSize"
                            Value="13" />
                </MultiTrigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <DockPanel>
        
        <akslToolkit:WindowTitle Title="{Binding Path=Title}" />
        
        <Grid DockPanel.Dock="Bottom"
              Margin="0,2,4,2">
            <StackPanel HorizontalAlignment="Right"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">
                <TextBlock Text="Total selected sales: " />
                <ContentPresenter Content="{Binding Path=TotalSelectedSales}"
                                  ContentStringFormat="c" />
            </StackPanel>
        </Grid>

        <Grid SnapsToDevicePixels="True">
            
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="2" />
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>
            
            <!--ItemsSource="{Binding Source={StaticResource groupCustomerSource}}"-->
            <ListView x:Name="customerListView"
                      Grid.Row="0"
                      AlternationCount="2"
                      ItemContainerStyle="{StaticResource customerItemStyle}"
                      ItemsSource="{Binding Path=AllCustomers}"
                      SelectionMode="Single"
                      SelectedItem="{Binding Path=SelectedCustomerItem,Mode=TwoWay}">

                <!--<behaviors:Interaction.Triggers>
                    <behaviors:EventTrigger EventName="Loaded">
                        <behaviors:CallMethodAction TargetObject="{Binding DataContext,ElementName=customerListView}"
                                                    MethodName="ListViewLoaded" />
                    </behaviors:EventTrigger>
                </behaviors:Interaction.Triggers>-->
                <ListView.GroupStyle>
                    <!--<GroupStyle HeaderTemplate="{StaticResource groupHeaderTemplate}" />-->
                    <GroupStyle ContainerStyle="{StaticResource customerGroupItemStyle}" />
                </ListView.GroupStyle>

                <ListView.View>
                    <GridView AllowsColumnReorder="True"  
                              ColumnHeaderContainerStyle="{StaticResource customerGridViewColumnHeaderStyle}">
                        <GridViewColumn Width="100"
                                        HeaderTemplate="{StaticResource checkBoxHeaderTemplate}">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding Path=IsChoice,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Name"
                                        Width="240"
                                        DisplayMemberBinding="{Binding Path=DisplayName}" />
                        <GridViewColumn Header="E-mail"
                                        Width="240"
                                        DisplayMemberBinding="{Binding Path=Email}" />
                        <GridViewColumn Header="Total Sales"
                                        Width="240">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <ContentPresenter Content="{Binding Path=TotalSales}"
                                                      ContentStringFormat="C"
                                                      HorizontalAlignment="Center" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>

            <!--<StackPanel Orientation="Horizontal"
                        Grid.Row="2">
                <TextBlock Text=" Filter Company Items "
                           VerticalAlignment="Center" />
                <CheckBox x:Name="companyFilterCheckBox"
                          IsChecked="{Binding Path=IsChecked,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                          VerticalAlignment="Center">
                    <behaviors:Interaction.Triggers>
                        <behaviors:EventTrigger EventName="Checked">
                            <behaviors:InvokeCommandAction Command="{Binding CompanyFilterCommand}"
                                                           CommandParameter="{Binding Path=ItemsSource,ElementName=customerListView}" />
                        </behaviors:EventTrigger>
                        <behaviors:EventTrigger EventName="Unchecked">
                            <behaviors:InvokeCommandAction Command="{Binding CompanyFilterCommand}"
                                                           CommandParameter="{Binding Path=ItemsSource,ElementName=customerListView}" />
                        </behaviors:EventTrigger>
                    </behaviors:Interaction.Triggers>
                </CheckBox>
                <Button Content="Remove Groups"
                        Command="{Binding Path=UnGroupCommand}"
                        CommandParameter="{Binding Path=ItemsSource,ElementName=customerListView}"
                        Margin="10,2,2,2" />
                <Button Content="Group by Project/Status"
                        Command="{Binding Path=GroupCommand}"
                        CommandParameter="{Binding Path=ItemsSource,ElementName=customerListView}"
                        Margin="2" />
            </StackPanel>-->
        </Grid>
    </DockPanel>
</UserControl>
