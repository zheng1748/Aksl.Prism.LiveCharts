<UserControl x:Class="Aksl.Modules.ListViewCustomer.Views.EditableCustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             x:Name="editableCustomerView"
             xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:actions="clr-namespace:Aksl.Xaml.Behaviors;assembly=Aksl.Toolkit"
             xmlns:views="clr-namespace:Aksl.Modules.ListViewCustomer.Views"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="800">
    <UserControl.Resources>

    </UserControl.Resources>
    
    <Grid Margin="4">
        <Grid Margin="4">
       
        <!--<Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>-->

        <views:EditableCustomerItemView Grid.Row="0" DataContext="{Binding Path=EditableCustomerItem}" />

        <!--<StackPanel Grid.Row="1"
            Orientation="Horizontal"
                        Background="WhiteSmoke"
                        HorizontalAlignment="Center">
                <Button x:Name="OkButton"
                        Content="保存"
                        Command="{Binding SaveCommand}"
                        Width="75"
                        Height="25"
                        Margin="0,1,20,1">
                </Button>
                <Button x:Name="CancelButton"
                        Content="取消"
                    Command="{Binding CloseCommand}"
                        Width="75"
                        Height="25"
                        HorizontalAlignment="Left"
                        Margin="10,1,0,1">
                </Button>
            </StackPanel>-->
       
    </Grid>
        <!--Grid.RowSpan="2"-->
        <ProgressBar Width="100"
                 Height="100"
                 VerticalAlignment="Center"
                 HorizontalAlignment="Center"
                 Visibility="{Binding Path=IsLoading,Mode=OneWay,Converter={StaticResource booleanToVisibilityConverter}}" />
    </Grid>
    
</UserControl>
