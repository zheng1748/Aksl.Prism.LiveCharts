<UserControl x:Class="Aksl.Modules.LiveCharts.Pies.Views.GaugeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="900"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>

    </UserControl.Resources>

    <Grid Margin="5,5,5,5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0"
                    Orientation="Vertical"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <TextBlock Text="Initial rotation"></TextBlock>
            <Slider Width="300"
                    Minimum="-360"
                    Maximum="720"
                    Value="{Binding InitialRotation,Mode=TwoWay}" />

            <TextBlock Text="Max angle"></TextBlock>
            <Slider Width="300"
                    Minimum="0"
                    Maximum="360"
                    Value="{Binding MaxAngle, Mode=TwoWay}" />

            <TextBlock Text="Inner radius"></TextBlock>
            <Slider Width="300"
                    Minimum="0"
                    Maximum="50"
                    Value="{Binding InnerRadius, Mode=TwoWay}" />

            <TextBlock Text="Offset radius"></TextBlock>
            <Slider Width="300"
                    Minimum="0"
                    Maximum="50"
                    Value="{Binding OffsetRadius, Mode=TwoWay}" />

            <TextBlock Text="Background inner radius"></TextBlock>
            <Slider Width="300"
                    Minimum="0"
                    Maximum="50"
                    Value="{Binding BackgroundInnerRadius, Mode=TwoWay}" />

            <TextBlock Text="Background offset radius"></TextBlock>
            <Slider Width="300"
                    Minimum="0"
                    Maximum="50"
                    Value="{Binding BackgroundOffsetRadius, Mode=TwoWay}" />
        </StackPanel>
        <lvc:PieChart Grid.Column="1"
                      Series="{Binding Series}"
                      MaxValue="{Binding GaugeTotal}"
                      MaxAngle="{Binding MaxAngle}"
                      InitialRotation="{Binding InitialRotation}" />
    </Grid>

</UserControl>
